- ファイルの変更は最小限にすること
- Effectを返す関数は`fn`を使い、`gen`を直接返す関数は作らないこと
- 検証は手動パースではなくSchemaを使うこと
- 説明は句点でつなげる長文にせず、Markdownの箇条書き`-`で記載すること
  - `- [ ]`のようなチェックボックスは使わないこと
- 複数の英単語を並べる場合に`/`の代わりに`, `により区切ること
  - 日本語が含まれる場合は`、`を使うこと
- `→`の代わりに`->`を使うこと
- コミットメッセージの形式は`feat: <変更内容>`または`fix: <変更内容>`とすること
- ワークツリー名及びブランチ名の形式は`feat/<タスク名のケバブケース>`または`fix/<タスク名のケバブケース>`とすること
