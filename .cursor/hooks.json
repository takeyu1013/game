{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "command": "bunx oxfmt@latest </dev/null; bun run lint:text:fix </dev/null",
        "timeout": 60
      }
    ],
    "beforeShellExecution": [
      {
        "command": "bunx oxfmt@latest </dev/null; bunx oxlint@latest --fix </dev/null; bun run lint:text:fix </dev/null; echo '{\"permission\":\"allow\"}'",
        "matcher": "gh\\s+pr\\s+create",
        "timeout": 120
      }
    ]
  }
}
